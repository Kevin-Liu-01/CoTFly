# Fourteen-tank recovery release — 2026-09-10

Status: final composed release **PASS**. This checkpoint publishes the fourteen
qualified vehicles below; the remaining fleet backlog is not declared complete.

## Final frozen-tree certificate

Candidate `dc180d2fc3a2a12a804873fdb2f226e605b13ffa`, incorporating published
`b4b1763be`, passed one uninterrupted official release from 10:53:02 to
11:35:17 UTC on 2026-09-10. The start and finish HEADs are identical. All twelve
stages passed, including fourteen strict-standard scores at least 92,
14/14 registered source comparisons (median 96.5), anatomy freshness,
centering, module alignment/hits, selected assets, duplicate-track detection,
muzzle bores, barrel circularity, full `npm test`, and private build.

The full npm lifecycle passed **933/933 entries**: 299 pre, 596 core, and 38
post. It used the maintained four-worker CPU runner with exclusive browser
barriers. This is a fresh complete run, not the historical failed prefixes
and diagnostic remainder below. Separate TypeScript/core-unused checks and
public build passed by 11:35:35 UTC. Scoped Doctor reported zero errors and
eight offline-test warnings across the skirt-paint, Leopard-roller, and Soviet
auxiliary-armor fixtures; this is not a warning-free claim.

Retained evidence in the recovery owner:
`.qa-dev/reports/fourteen-tank-publication-grkSRL/{receipt.json,release.log,types.log,doctor.log,public-build.log}`.
The publication index was compared byte-for-byte against the qualified tree.
Only this final documentation and the gate ledger's generation timestamp change
after qualification; no runtime, test, asset, or geometry packet is substituted.
The earlier failed attempts remain documented rather than relabeled successful.

One composed release covers all fourteen tanks. It does not repeat the entire
fleet suite fourteen times, remove assertions, waive failed geometry, or ship
source models and temporary QA. The ordinary FIFO lease remains in force.

## Bounded source scope

- Leclerc X / Classic X: efficient fully capped measured-link pins; Leclerc
  LOW uses 16-sided stepped wheel solids, HIGH retains 32.
- Strv 122 X: measured 48-sided wheels, retaining the original surface tolerance.
- Leopard 2A4M X / 2A5 X / KF51 X: fitted return rollers and attached supports;
  A5 also has its measured inner upper-carrier lining.
- AMX-30 X / T-72B 1987 X / B3 X / BU X / T-62MV-1 X / T-90 X / Burlak X /
  MS X: open annular tires expose the actual painted wheel faces.
- T-90 X / T-72BU X / T-62MV-1 X: fixed skirts use body paint. T-72B3 X also
  closes the upper shoulder gap without hiding the lower wheel faces.
- T-90 X: shared round red Shtora, mounted to physical carriers, and a complete
  roof weapon in its rearward-stowed pose. The separate optical sensor remains
  distinct from the emitters. Earlier failed weapon arrangements stay excluded.

These changes do not finish country-matched track thickness, all fleet
triangle budgets, every fender, or the 59-vehicle style/performance backlog.
T-72B3M, failed fitted T-80U/T-90A pilots, and inferred Merkava station counts
are not silently included. Source models and temporary QA are excluded.

## Retained first composed release

At `8b3e8c8ac9b845318e3ca09522896a9801b7882c`, the complete anatomy update,
selected asset generation, and anatomy check passed. Official release phases
1–10 passed: strict standard (all fourteen at least 92), available-reference
fidelity (14/14, median 96.5), anatomy freshness, centering, module alignment,
module hits, assets, duplicate tracks, muzzle bores, and barrel circularity.
All fourteen fresh neutral comparison boards were inspected. These comparisons
are not a benchmark of switching latency or proof of every concealed joint.

Phase 11 stopped in `sourceXOtherAuxArmor.selftest.mjs`: its complete pre-repair
AMX-30 fingerprint correctly noticed the intentional annular-tire change.
The overall release is **FAIL**. The full npm suite, private build, and remaining
outer driver phases did not pass in this attempt. Receipt and logs are retained
under `.qa-dev/reports/fourteen-tank-publication-79okq6/` in the recovery owner.

## Compatibility repair, not new goldens

The original fourteen whole-model hashes are unchanged. Test-only witnesses
invert precisely AMX-30's `.314` tire opening, both Leclercs' four 8-sided pins
to their original 12-sided recipe, and Leclerc LOW's authenticated 16-sided
wheel buffers to the original 32-sided solids. Every other geometry, material,
instance, and owner remains in the complete historical hash. Existing declared
fixed-sheet finish inverses still compose with these witnesses.

Current physical armor, seam, air, and projectile checks continue to use the
actual candidate, not historical geometry. Negative controls reject wrong IDs,
changed opt-ins, custom shoes, missing native calls, and a 1 mm pin corruption.
Near/far disposal checks preserve caller-owned output and one disposal per
owned intermediate. Independent review identified a double-disposal in the
first helper; the non-consuming merge fixes it without relaxing any assertion.

Production vehicle source is unchanged from the first composed release; only
compatibility tests and published game pacing were integrated afterwards. The
successful anatomy update/asset generation therefore remains applicable. A new
uninterrupted official release, including its own anatomy freshness check and
full npm lifecycle, is still required before publication.

The five-entry composed compatibility run passed: complete other-family and
Soviet auxiliary-armor suites, AMX-30 native geometry, Leclerc native geometry,
and measured Leclerc track links. New helper metrics passed (19 functions,
zero violations or explicit `any`/`unknown`). The corrected helper's independent
review found no remaining blocker. This focused result is not a full npm pass.

## Second composed release and complete compatibility preflight

The frozen `225a645448b4af944d65b6b17e5b02eba6b21f01` retry again passed all ten
geometric release phases, including 14/14 reference fidelity (median 96.5).
Phase 11 stopped at the original Strv 122 X whole-model fingerprint in
`sourceXWesternAuxArmor.selftest.mjs`, which still expected 72-sided wheels.
This attempt is also **FAIL**, not a complete npm or build pass. Its evidence
is retained under `.qa-dev/reports/fourteen-tank-publication-mEQHBL/`.

The test-only Strv witness authenticates the actual 48-sided wheel buffers
before reconstructing the original 72-sided fingerprint. It preserves the
original goldens, caller ownership, and current-candidate physical checks.
Negative controls reject changed wheel faces and missing native calls and
verify hook restoration and one disposal per owned buffer. The same witness
repairs the old non-target cupola fingerprint without altering its source-air
or contact checks. Its metrics pass with eight functions and zero violations.

A bounded 25-file compatibility preflight then exposed two more old scene
assumptions: T-62MV-1 X's sight fingerprint included the intentionally changed
tires/skirts, and T-90 X's side-mount rays still selected the old rubber bucket.
The sight test now uses authenticated historical witnesses only for the old
golden. The side-mount test explicitly requires the painted bodywork bucket
and armor-paint role, with unchanged rays and contact limits. Material-array
comparison now checks every slot and geometry group, including negative
controls for a changed second material or group assignment.

All 25 files pass in one fresh preflight invocation; receipt:
`.qa-dev/fourteen-fixture-preflight-rYtuxM/receipt.json`. Earlier failed
preflights remain recorded. Independent review found no blocker in these
test-only changes and no weakened original golden or physical assertion.
This result is not a full npm pass. No runtime geometry changed in this repair.

All fourteen current colored angle assets were also visually inspected.
T-90 X visibly has its two round red emitters and painted wheel faces/skirts.
These previews do not certify concealed joints, complete country track-gauge
matching, or the remaining dark T-72B3 skirt panels; those broader tasks remain
open.

## Third composed release: declared fitting changes versus source rays

At `54b3698453404658faccea0e2e78bb6321a3fe83`, all ten geometric phases again
passed, including the full anatomy freshness check. The uninterrupted npm run
then passed 204 entries and failed two T-90 X fixtures (entries 205/206).
`t90AwXCheekLeaves` still aimed original whole-source rays through space now
occupied by the intentionally taller canonical Shtora hood. The isolated APS
fixture also lacked the material/group port required by that current assembly.
The release is **FAIL**, retained in
`.qa-dev/reports/fourteen-tank-publication-KoPPik/`; neither the full npm suite
nor the remaining build phases is certified by this attempt.

The repaired callers retain all 87 cheek and 83 APS immutable source rays via
the existing authenticated Shtora/added-NSVT historical witness. This witness
does not blanket-exclude foreground geometry. Current ERA buffers must remain
identical to the witness, and actual current air, gameplay hit/strip/spent/reset,
permanent backing, and physical APS clamp/receiver tests remain separate.
The current receiver is 0.3645 m tall, so inside-solid rays now reach that
individual primitive's full measured height, require the point inside its
actual bounds, and reject points above/below it. This is not a relaxed contact
gap tolerance; both opposite surfaces must enclose the same clamp point.

Independent review found no weakened oracle or physical assertion. Its two
cleanup findings were repaired: historical construction now participates in
the current tank's cleanup, and paired red lenses dispose their one shared
material once. A focused ownership fixture checks success, rejection of a
1 mm corrupted emitter, and preservation of borrowed native material.
The four-entry T-90 cheek/APS/Shtora/Soviet-auxiliary run passes in
`.qa-dev/t90-fitting-fixtures-r3.log`. This is not a full npm pass.

To expose remaining failures together, an explicitly diagnostic run executed
all 726 unexecuted catalog entries: **726 PASS, zero failures**, receipt
`.qa-dev/fourteen-remainder-JV30O6/receipt.json`. This does not combine the
failed prefix and resumed tail into an uninterrupted npm PASS. Final
qualification still requires a fresh complete release. The candidate then
merged published `33b243436` (bounded deployment uploads and grounded shrub
crowns), retaining its new test; the composed catalog now has 933 entries.
