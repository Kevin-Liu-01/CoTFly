# September 11 launch checkpoint

The public site is running `v1.0.0+gcd0b28411` at
https://cot.kevinliu.studio. Deployment `dpl_D6M2HkmegAYkme8xbpQKFabDueHT`
was built with the explicit source revision, promoted after regression checks,
and verified through the public domain. This is a verified checkpoint, not
completion of the entire fleet performance program.

## Included changes

The Challenger 3 X rear closure remains intact and the standard Challenger 3
is preserved. Strv 122 X uses the qualified lower-cost track links and matching
technical cards. K2 X has fitted return rollers and corrected cannon stock;
T90SM X retains visible painted wheel faces with neutral rubber. CV90, CV90
Mk IV and Type89 Light Tiger have corrected structural-panel paint.

The checkpoint also includes staged loading/FX ownership, shared soot textures,
Garage retention and clutter culling, restored Studio timelines and recording,
room-picker focus containment, completed map-road approaches, road-clear rim
trees, cliff-normal corrections, the bounded Redrock seam repair, and remapped
Tidal vegetation receipts. Battle builds share byte-identical rigid wheel,
fitting and marking geometry while retaining separate disposal owners.

## Verification and scope

- `7f1b8358a`: complete Strv release procedure, source/fidelity/contact/anatomy/
  assets, all 311 pre / 648 core / 41 post checks, and private build pass.
- `fb3b04061`: complete 311 pre / 649 core / 42 post lifecycle passes after
  immutable geometry sharing. Native four-view RGBA equality, sibling disposal,
  moved-stock negative control, and actual articulation/wreck/reset pass.
- `cd0b28411`: holds the unqualified T72B3M wheel change; seven affected checks,
  selected assets, typecheck and public build pass. The local public build
  passes fresh native two-peer invite/ready/start/countdown/input/exit checks.
- The cloud build has different bundle hashes from the local build and was
  verified separately. Native public-domain two-peer controls, active snapshot/
  input progression, exit and room closure pass without application errors.
  Cloudflare signaling/ICE dependency checks and actual TURN allocation pass
  with ten UDP relay candidates in a pristine browser. This is not evidence
  of distinct physical networks or a clean frame-time qualification.
- Nine selected public portraits match the exact publication manifest.
- Thirty-map collision, 2,100 spawn, 480 route, and scoped matched visual
  checks passed before integration; current full regression passes as above.
- All twenty Studio scene durations and eleven completed promo exports pass
  their documented checks. Media artifacts remain local; no social publishing
  or sustained 60 fps rendering claim is made.

The protected deployment URL failed to create a room. The signaling origin
policy allows only the canonical public origin, which explains that failure;
this attribution is an inference from the configuration. The failed attempt is
retained; the public-domain test passed before and after promotion. The first
remote build omitted the revision label and was never promoted; the promoted
build supplies `COT_BUILD_REVISION` explicitly.

## Open requirements

The eight-second phase-resource gate still fails four independent ceilings:

| Resource | Observed | Limit |
|---|---:|---:|
| Garage idle triangles | 264,999 | 240,000 |
| Garage returned triangles | 268,705 | 240,000 |
| Battle uploaded geometries | 716 | 680 |
| Battle material owners | 235 | 220 |

Attached battle geometries improve from 708 to 664 and pass their 680 ceiling.
The shorter sample's 608 uploaded geometries is not substituted for the longer
warmed result. Shared clones retain additional bounded CPU backing; this is
not a CPU-allocation reduction. No thresholds, palettes, shadows or near track
features were weakened to obtain a pass. Clean timing remains unqualified on
the contended host, and 46 of 63 measured tanks still show at most 10 percent
HIGH-to-LOW triangle reduction.

T72B3M X keeps its previous shipped profile, images and manifest row exactly.
Its wheel-paint draft remains preserved separately pending owner adjudication
of the preexisting source-real open-channel versus zero-continuity conflict.
Chieftain Mk5 X fitted tracks/rotors and refreshed anatomy are preserved at
`0f95db4d8`; the missing required roof gun remains an unresolved owner decision,
and this draft is not included. Challenger 3 X repair evidence does not create
a missing source-fidelity oracle.

The manual GitHub production workflow still has an invalid Vercel token. The
authenticated CLI can deploy, but the supported token-creation API rejects its
application with HTTP 403. No new token was created and no GitHub secret was
changed. The direct authenticated deployment path was used for this checkpoint.

Detailed transient receipts remain in the owned worktrees under `.qa-dev/launch`;
raw source models, credentials, generated QA and unfinished experiments were
not added to the publication commit.
